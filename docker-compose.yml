services:
  backend:
    build: ./backend
    env_file: backend/.env  # compose parses quotes correctly, unlike `docker run --env-file`
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
